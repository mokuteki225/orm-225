import { PostgresConnectionOptions } from '../../database-adapters/postgres/interfaces/PostgresConnectionOptions';

/**
 * Union type of database connection options
 */
export type DatabaseConnectionOptions = PostgresConnectionOptions;
